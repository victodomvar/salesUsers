apiVersion: v1
kind: Service
metadata:
  name: salesusers
  namespace: deibidsales
  labels:
    app.kubernetes.io/name: salesusers
    app.kubernetes.io/part-of: salesusers
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: salesusers
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
